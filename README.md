# Neural Network Django Web App

A simple neural network designed to detect handwritten digit.

NN was integrated with django web app so user can upload handwritten picture of a digit to detect the number.
